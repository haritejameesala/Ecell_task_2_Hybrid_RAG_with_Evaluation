# Exploratory Notebooks

This folder is for exploratory / scratch work only -- Jupyter notebooks used
while testing text parsing strategies, chunking parameters, and prompt
architecture during development.

Production pipeline code lives in `src/` and `api/`; nothing here is
imported by the running application. Runtime data (uploaded PDFs and
serialized FAISS indices) lives under `data/notebooks/<name>/`, not here.

